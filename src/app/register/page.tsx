import { PublicLayout } from "@/components/layout";
import { EmptyState } from "@/components/layout/empty-state";

export default function RegisterPage() {
  return (
    <PublicLayout>
      <EmptyState title="Register" description="Placeholder do cadastro." />
    </PublicLayout>
  );
}
